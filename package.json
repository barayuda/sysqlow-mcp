{
  "name": "sysqlow-mcp",
  "version": "1.0.0",
  "description": "Intelligent, self-validating engineering knowledge base MCP server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --external fastmcp --external @libsql/client --external zod",
    "compile": "bun build ./src/index.ts --compile --outfile ./dist/sysqlow-mcp --external sury --external @valibot/to-json-schema --external effect --external arktype --external typia --external valibot",
    "test:workflow": "bun run src/test-workflow.ts",
    "prepare": "husky || true"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "fastmcp": "^4.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^20.12.7",
    "husky": "^9.1.7",
    "typescript": "^5.4.5"
  }
}
